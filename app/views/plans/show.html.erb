<%- model_class = Modification -%>
<div class="page-header">
  <h1><%=@plan.name%>——资管</h1>
</div>
<table class="table table-striped" , data-toggle="table">
  <thead>
    <tr>
      <th>名称</th>
      <th>类型</th>
      <th>开始日期</th>
      <th>结束日期</th>
      <th>总管理费</th>
      <th>本年已计提管理费</th>
      <th>当前规模</th>
      <th>流动性规模</th>
      <th>部门</th>
      <th>费率</th>
      <th>计划管理人</th>
      <!-- 
      <th><%= model_class.human_attribute_name(:charge_type) %></th>
      <th><%= model_class.human_attribute_name(:charge_amount) %></th>
      
      <th>费用金额</th>
      <th>合作者</th> 
    -->
      <th>归属类型</th>
      <th>合作伙伴</th>
      <th>产品模式</th>
      <th>注释</th>
      <th>操作</th>
    </tr>
  </thead>

  <tbody>
    
      <tr>
        <td><%=link_to @plan.name , plan_path(@plan), :class=>"btn btn-link" %></td>
        <td><%=@plan.plan_type %></td>
        <td><%=@plan.start_date %></td>
        <td><%=@plan.end_date %></td>
        <!--
        <td><%= f2 @plan.m_whole_fee %></td>
        <td><%= f2 @plan.m_year_fee %></td>
        <td><%= f2 @plan.m_scale %></td>
        <td><%= f2 @plan.mm_scale %></td>
        -->
        <td><%= f2 @plan.whole_plan_fee %></td>
        <td><%= f2 @plan.this_year_fee %></td>
        <td><%= f2 @plan.count_plan_scale %></td>
        <td><%= f2 @plan.mobility_scale %></td>
        <td><%=@plan.department.name %></td>
        <td><%=@plan.rate %></td>
        <td><%=@plan.user.username %></td>
        <!-- 
        <td><%=@plan.charge_type %></td>
        <td><%=@plan.charge_amount %></td>
        
        <td><%=@plan.fee %></td>
        <td>|
          <% @plan.cooperations.each do |co|%>
            <%= "#{co.user.username} #{co.ratio}" %>|
          <% end %>
        </td> -->
        <td><%=@plan.ownership_type %></td>
        <td><%=@plan.parter %></td>
        
        <td><%=@plan.model_type %></td>
        <td><%=@plan.notes %></td>
        <!--
         <td>
          <%= link_to :编辑,
                      edit_plan_path(@plan), :class => 'btn btn-default btn-xs' %>
          <%= link_to :删除,
                      @plan,
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>

        -->
      </tr>
    
  </tbody>
</table>
<!-- <%= link_to 'Projects', plan_projects_path(@plan) %> |  -->
<%= link_to '编辑', edit_plan_path(@plan), :class=>"btn btn-link" %> |
<%= link_to '返回', plans_path, :class=>"btn btn-link" %>


<% @projects=@plan.projects %>
<%= render :partial => "/homes/projects" , :locals => { :projects => @projects, :name => "资管计划" ,:editable => false} %>
<%= link_to '新建项目', new_plan_project_path(@plan) ,:class => 'btn btn-primary'%>

<%= render :partial => "/modifications/index", :locals => { :object => @plan} %> 